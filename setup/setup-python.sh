# can't use a normal requirements.txt because of weird /cvmfs stuff so here we are...

python -m pip install --isolated -U numpy
python -m pip install --isolated -U pandas
python -m pip install --isolated -U uproot
python -m pip install --isolated -U sklearn
python -m pip install --isolated -U tensorflow
python -m pip install --isolated -U keras
